[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "financeos-deposit-parser"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "psycopg2-binary>=2.9.0",
    "pdfplumber>=0.11.0",
    "python-dateutil>=2.9.0",
]

[tool.hatch.build.targets.wheel]
packages = ["backend"]
